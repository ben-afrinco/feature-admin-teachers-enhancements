# Function: markAsRead

## Path
`app/Http/Controllers/NotificationController.php`

## Description
Marks a specific notification as "read" for the current user.

## How it works in code
- Updates the `read_at` timestamp for the notification ID.

## How to use from Browser
- Click on a notification in the notification bell/list to mark it as read.
