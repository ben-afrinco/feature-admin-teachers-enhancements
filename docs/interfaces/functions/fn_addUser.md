# Function: addUser

## Path
`app/Http/Controllers/LandingController.php`

## Description
Adds a new user to the system (Admin only).

## How it works in code
- Validates user input (name, email, role, password).
- Creates a record in `User_Model`.

## How to use from Browser
- Used in the Admin Dashboard under the "Users" management tab.
