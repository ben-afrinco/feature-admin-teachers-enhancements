# Function: saveNote

## Path
`app/Http/Controllers/GradeController.php`

## Description
Saves qualitative teacher feedback (notes/comments) for a student's submission.

## How it works in code
- Updates the `note` text field on the `Submission` database record.

## How to use from Browser
- Teachers type feedback in the "Instructor Comments" box and click "Save Note".
